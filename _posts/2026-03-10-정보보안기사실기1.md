---
## title: "[정보보안기사] 1.시스템보안"
date: 2026-03-10 05:00:00 +0900
---

| 분류 (주제) | 문제 | 답 |
| :--- | :--- | :--- |
| **계정 및 패스워드 관리** | 리눅스, AIX, HP-UX, Solaris 등 각 OS별 **패스워드 최소 길이(8자리)** 설정 파일 및 설정값은? | - **Solaris:** `/etc/default/passwd` (PASSLENGTH=8)<br>- **Linux:** `/etc/login.defs` (PASS_MIN_LEN 8)<br>- **AIX:** `/etc/security/user` (minlen=8)<br>- **HP-UX:** `/etc/default/security` (MIN_PASSWORD_LENGTH=8) |
|  | 리눅스의 **PAM 모듈** 중 다음 역할을 담당하는 모듈은?<br>1) 실질적 인증(패스워드 확인)<br>2) 시스템 사용 권한 확인<br>3) 인증 성공 시 세션 연결 | 1) `auth`<br>2) `account`<br>3) `session` |
|  | 유닉스의 `/etc/passwd` 등록 정보 중<br>`test01:x:100:1000:/home/exam:/bin/bash`<br>에서 1000, /home/exam, /bin/bash 의 의미는? | 1) **1000:** GID(그룹 ID)<br>2) **/home/exam:** 사용자 홈디렉터리<br>3) **/bin/bash:** 로그인 쉘 |
|  | **윈도우 OS의 계정 그룹** 중 다음의 명칭은?<br>1) 관리자보단 적지만 일반보단 많은 권한<br>2) 시스템 백업 목적<br>3) 일반 사용자 그룹 | 1) Power Users<br>2) Backup Operators<br>3) Users |
|  | `/etc/shadow` 파일 내 암호화된 패스워드 값의<br>맨 첫 번째 항목(`x:a$b$c`)의 **의미**와<br>**`b` 값(Salt)**이 레인보우 테이블 공격을 막을 수 있는 이유는? | - **첫 번째 항목(a):** 암호화에 사용된 해시 알고리즘 (1=MD5, 5=SHA256, 6=SHA512)<br>- **Salt 역할:** 해시 연산 시 Salt 값을 추가 입력값으로 사용하여, 동일한 암호라도 해시값이 달라지게 만들어 레인보우 테이블 공격 방어 |
|  | 유닉스 계정 패스워드 **임계값 설정 옵션**의 의미는?<br>(`deny=5`, `unlock_time=120`, `no_magic_root`, `reset`) | - **deny=5:** 인증 실패 5회 시 계정 잠김<br>- **unlock_time=120:** 잠김 120초 후 자동 해제<br>- **no_magic_root:** root 계정은 잠김 예외<br>- **reset:** 접속 성공 시 실패 횟수 초기화 |
|  | **`pwunconv`** 명령어의 기능은? | `/etc/shadow`의 패스워드 값이 `/etc/passwd` 파일로 들어가고 shadow 파일은 비활성화됨 |
| **파일 권한 및 접근 통제** | **특수 권한 비트**가 설정된 다음 파일 권한의 의미는?<br>1) `-r-sr-xr-x root sys /usr/bin/passwd`<br>2) `-r-xr-sr-x root mail /usr/bin/mail`<br>3) `drwxrwxrwt sys sys /tmp` | 1) **SetUID 설정 (s):** 일반 사용자가 실행해도 소유자(root) 권한으로 실행됨.<br>2) **SetGID 설정 (s):** 실행 시 그룹(mail) 권한으로 실행됨.<br>3) **Sticky bit 설정 (t):** 누구나 읽기/쓰기가 가능하나, 파일 삭제는 소유자와 root만 가능 |
|  | 1) 특수 권한(`SetUID`, `SetGID`) 해제 명령어<br>2) root 소유 특수 비트 설정 파일 검색 명령어<br>3) 이 조치가 필요한 이유는? | 1) `chmod -s {파일명}`<br>2) `find / -user root -type f \( -perm -4000 -o -perm -2000 \) \| xargs ls -al`<br>3) 악의적 특수 비트 파일 실행 시 일반 사용자도 root 권한을 얻어 악의적 행위가 가능하기 때문 |
|  | `getresuid()` 호출 시 ruid, euid, suid가 (A)이고, `setuid(600)` 이후 다시 호출하면 (B), (C), (C)이다. 값은? | (A) 0<br>(B) 0<br>(C) 600 |
|  | 윈도우 사용자 계정 컨트롤(UAC) 창에서 cmd.exe 실행 시 **[예] 버튼이 비활성화된 이유**는? | - 윈도우 관리자 계정이 정상이 아닌 경우(비활성화, 삭제 등)<br>- 윈도우 시스템 파일이 손상된 경우 |
|  | `umask`를 266으로 설정하고 백업 후, **권한을 원래대로** 돌리는 명령어는? | `umask 022` |
| **로그 및 프로세스 관리** | 유닉스/리눅스 시스템 **주요 로그 파일** 용도<br>1) 최근 로그인 시간 및 호스트<br>2) su 명령어 사용 기록<br>3) 시스템 모든 명령어 실행 기록<br>4) 현재 로그인 사용자 상태<br>5) 로그인/로그아웃/재부팅 기록<br>6) 5회 이상 로그인 실패 기록 | 1) lastlog<br>2) sulog<br>3) acct/pacct<br>4) utmp<br>5) wtmp<br>6) btmp (또는 loginlog) |
|  | 유닉스에서 현재 실행 중인 프로세스 정보가 기록되어 숨겨진 프로세스를 찾기 위해 참조하는 **디렉터리 경로**는? | `/proc` |
|  | `/etc/apache/conf` 디렉터리 내에서 **10일 이내에 수정된 파일**을 찾기 위한 명령어는? | `find /etc/apache/conf -type f -mtime -10` |
|  | 프로그램에서 실행하는 시스템 콜을 추적하여 트로이목마 감염 파일 등을 **디버깅할 수 있는 명령어**는? | `strace` |
|  | 리눅스 `logrotate.conf` 설정 중 다음 옵션은?<br>1) 주 단위 순환<br>2) 오래된 로그 순환 후 새 파일 생성<br>3) 압축 저장 | 1) weekly<br>2) create<br>3) compress |
|  | 리눅스 파일 시스템(ext3 이상)에서 갑작스러운 시스템 종료 시 기존 작성된 로그를 통해 복구하는 기법은? | 저널링 (Journaling) |
|  | 백도어 프로세스(`/proc/5900`) 파일이 삭제되었을 때,<br>1) 복구하는 명령어<br>2) 공격자가 사용한 명령어 확인 방법 | 1) `cp /proc/5900/exe /tmp/backdoor`<br>2) `history` 명령 또는 `cat /proc/5900/cmdline` 확인 |
| **서비스 통제 및 스케줄링** | **`xinetd`** 서비스 환경설정(`telnet`) 옵션<br>1) 서비스 미허용 IP 대역<br>2) 서비스 허용 IP 대역<br>3) 동시 접속 가능한 최대 세션 수<br>4) 초당 연결 개수 및 제한시간 | 1) `no_access`<br>2) `only_from`<br>3) `instances`<br>4) `cps = 10 5` (초당 10개 제한, 초과 시 5초 제한) |
|  | 네트워크 서비스 통제를 위한 UNIX 방화벽 룰 도구 명칭 및 허용/거부 설정 파일 명칭은? | - **도구:** `tcpwrapper`<br>- **허용 파일:** `hosts.allow`<br>- **거부 파일:** `hosts.deny` |
|  | `rsh`, `rlogin`, `rexec` 등 r-command 사용 시 `/etc/hosts.equiv`, `.rhosts` 파일의 안전한 보안 설정 방법은? | 1) 소유자를 root 또는 해당 계정으로 변경 (`chown root`)<br>2) 파일 권한을 600 이하로 변경 (`chmod 600`)<br>3) 파일 내 `+`를 제거하고 허용할 특정 호스트 및 계정만 명시 |
|  | **root 원격 접속을 제한**하기 위한 Solaris, AIX, Linux의 설정 파일은 각각 무엇인가? | - **Solaris:** `/etc/default/login`<br>- **AIX:** `/etc/security/user`<br>- **Linux:** `/etc/securetty` |
|  | **`crontab`** 스케줄러 설정<br>1) 조회 명령어<br>2) 사용자(sis) 편집 명령어<br>3) 매주 일요일 새벽 3시 `/home` 하위 삭제 등록 | 1) `crontab -l`<br>2) `crontab -u sis -e`<br>3) `0 3 * * 0 rm -rf /home/ 1> /dev/null 2> &1*` |
| **악성코드 분석 및 포렌식** | **디지털 포렌식 5대 원칙**을 설명하시오. | 1) **정당성:** 적법한 절차에 따라 수집<br>2) **재현성:** 동일 조건에서 동일한 결과 산출<br>3) **신속성:** 메모리 등 휘발성 정보 확보를 위한 지체 없는 대응<br>4) **연계보관성:** 수집~제출 각 단계의 명확한 책임 연계 보장<br>5) **무결성:** 증거 위변조 방지 및 원본 동일성 증명 |
|  | **윈도우 PE 파일**의 Section 영역 명칭<br>1) 코드 정보 저장<br>2) 전역변수/상수 저장<br>3) DLL 함수 정보 저장 | 1) `.text`<br>2) `.data`<br>3) `.idata` |
|  | 악성파일 분석 기법 3가지 (자동화, 반자동화, 수동)의 개념은? | 1) **자동화:** Cuckoo Sandbox 등의 도구로 행위 및 패턴 자동 분석<br>2) **반자동화:** 자동화 도구 분석 후, 의심 코드를 디스어셈블러로 분석<br>3) **수동:** 디버거, 디스어셈블러로 기계어/어셈블리 코드를 직접 추적 |
|  | 익스플로잇 코드 구성 중 다음의 명칭은?<br>1) 본체에 해당하는 기계어<br>2) NOP의 Hex코드<br>3) ESP 값을 EIP로 옮기는 어셈블리 명령 | 1) 쉘코드 (Shell Code)<br>2) `0x90`<br>3) `RET EIP ESP` (또는 `JUMP ESP`) |
|  | 악성코드 감염 방식 중,<br>(A) 일반 다운로더와 달리 사용자 모르게 설치되는 파일<br>(B) (A)와 같으나 메모리에 상주하는 형태 | (A) 드롭퍼 (Dropper)<br>(B) 인젝터 (Injector) |
